import __ from './blank'
/**
 * equals
 *
 * Create an equals function that
 * compares two values and returns
 * true if the values are equal
 * and false if they are not
 */
const equals = __

export default equals
